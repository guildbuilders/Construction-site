# The A pages keep their filenames - they are the final URLs in Google Ads,
# and an ad should land on the page itself rather than on a redirect.
# Only the retired B pages redirect, so any B ad still in flight lands on A
# instead of a 404. The suffix-less forms are here because they were briefly
# live and may have been crawled or cached.
/kitchen-remodel-b       /kitchen-remodel-a  301
/kitchen-remodel-b.html  /kitchen-remodel-a  301
/kitchen-remodel         /kitchen-remodel-a  301
/bathroom-remodel-b      /bathroom-remodel-a 301
/bathroom-remodel-b.html /bathroom-remodel-a 301
/bathroom-remodel        /bathroom-remodel-a 301

/exterior-renovations.html /decks-patio-covers 301
/exterior-renovations /decks-patio-covers 301

# A sitemap was submitted to Search Console at this path by mistake and the
# UI gives no way to remove it. Point it at the real sitemap index so the
# entry reports success instead of a permanent 404 error.
/guildbuildersgroup.com /sitemap-index.xml 301
